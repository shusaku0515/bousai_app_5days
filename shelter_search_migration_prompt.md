# 避難所検索結果画面の実装依頼

既存システムに、以下の仕様を満たす「避難所検索結果画面」だけを実装してください。

既存のフレームワーク、ルーティング、テンプレート方式、データ保存方式は対象システムに合わせてください。既存機能や無関係な画面は変更しないでください。実装に必要なファイルを確認したうえで、質問せず、この指示だけで最後まで実装・動作確認してください。

## 1. 避難所データ

避難所は次の属性を持てるようにしてください。属性値は検索に使える boolean とします。

- `shortest`: 最短
- `barrier_free`: バリアフリー
- `parking`: 駐車場あり
- `nursing_room`: 授乳室
- `pets_allowed`: ペット可
- `universal_design`: ユニバーサルデザイン
- `prayer_room`: 礼拝室あり
- `air_conditioning`: 空調完備
- `large_facility`: 大規模施設

設備検索画面では、上記の条件を複数選択できるようにしてください。

## 2. 避難所登録からの情報入力

満員情報と設備情報は、検索結果画面に固定値として埋め込まないでください。避難所登録画面から入力を受け取り、避難所データとして保存し、検索結果画面で保存値を表示してください。

登録フォームには次の入力項目を追加してください。

- 避難所名（必須）
- 空き状況: `満員`、`余裕あり`、`普通` のいずれか
- 設備・条件: 上記の設備属性を複数選択できるチェックボックス

登録時の処理要件:

- 選択された設備だけを boolean の `true` として保存する。
- 未選択の設備は、未設定または `false` として扱う。
- 空き状況の未選択・不正値は `普通` として扱う。
- 許可されていない設備キーは保存しない。
- 登録後に保存データを読み直しても、空き状況と設備情報が維持される。
- 登録画面の入力値は、既存の保存方式（JSON、データベースなど）に合わせて永続化する。

## 3. 検索結果の対象と並び順

- 避難所名での部分一致検索に対応する。
- 設備条件を複数選択できるようにする。
- 選択した設備条件のうち、1つ以上に一致する避難所を検索結果の上位に表示する。
- 各避難所について、選択条件に一致した数を数える。
- 一致数が多い避難所から順に表示する。
- 一致数が同じ場合は、現在地が取得できれば現在地から近い順にする。
- 現在地が取得できない場合は登録順など既存の安定した順序を維持する。
- 選択条件に1つも一致しない避難所も結果から除外せず、下位に表示する。
- 設備条件を何も選択しない検索でも、登録されている全避難所を表示する。

## 4. 施設カードの色

設備条件を選択して検索した場合、施設カード全体を一致状況で表示分けしてください。

- 1つ以上の条件に一致する施設: 有色のカード
- 1つも条件に一致しない施設: 無彩色のカード

検索条件を何も選択していない場合は、全施設を通常の無彩色カードとして表示してください。

## 5. 設備タグの色

施設カード内には、その避難所が持っている設備をタグで表示してください。

設備条件を選択して検索した場合:

- 今回選択した条件に一致する設備タグ: 有色
- 登録されているが、今回選択した条件ではない設備タグ: 無彩色

設備条件を何も選択していない場合:

- 表示されるすべての設備タグを無彩色にする

設備を持っていない避難所には、存在しない設備タグを表示しないでください。

## 6. 空き状況表示

各避難所に `availability` の状態を持たせ、施設名の横に表示してください。

状態ごとの表示は次のとおりです。

| 状態 | 色 | 顔マーク |
|---|---|---|
| `満員` | 赤 | 3体 |
| `余裕あり` | 青 | 1体 |
| `普通` | 黄色 | 2体 |

顔マークは視認できる顔の絵文字または既存システムの顔アイコンを使ってください。色だけに頼らず、状態名と顔マークも必ず表示してください。

## 7. 既存データとの互換性

既存データにすでに次の例がある場合は、表示確認用の初期データとして保持して構いません。ただし、アプリの処理や画面表示をこれらの名前に固定してはいけません。新しく登録された避難所は、必ず登録フォームから受け取った値を使ってください。

- 名前が「高校」の避難所: `availability = "満員"`
- 「片瀬小学校」: `availability = "余裕あり"`
- 上記以外のすべての避難所: `availability = "普通"`

設備属性の初期データは次のとおりです。

- 「御所見小学校」: `parking = true`, `pets_allowed = true`
- 「片瀬小学校」: `shortest = true`, `barrier_free = true`, `parking = true`, `nursing_room = true`, `pets_allowed = true`, `universal_design = true`, `prayer_room = true`, `air_conditioning = true`
- 「xyz」: `barrier_free = true`, `air_conditioning = true`, `large_facility = true`

## 8. 実装上の注意

- 既存のデータ形式、画面デザイン、命名規則を優先する。
- 設備キーと画面表示名の対応は、1か所で管理できるようにする。
- 不正な検索条件は無視し、エラーにしない。
- 検索条件未指定時に、全施設が消えたり、設備タグが有色になったりしないようにする。
- 満員情報が未設定の既存データがあっても画面が壊れないよう、`普通` などの妥当な既定値を使う。
- モバイル幅でもカード、設備タグ、空き状況が崩れないようにする。
- 既存の現在地検索や距離表示がある場合は維持する。
- 満員情報や設備情報を、施設名に応じてコード内で自動設定する処理は追加しない。
- 検索結果は登録・保存された `availability` と設備属性を参照して描画する。

## 9. 完了条件

実装後、次を確認してください。

1. 設備条件を1つ選んだ検索で、一致施設が上位になり、一致しない施設も下位に残る。
2. 複数条件の検索で、一致数の多い施設が先に表示される。
3. 一致施設のカードが有色、非一致施設のカードが無彩色になる。
4. 一致した設備タグだけが有色になり、他の設備タグが無彩色になる。
5. 条件なし検索では全施設が表示され、設備タグがすべて無彩色になる。
6. 高校は赤色・満員・顔マーク3体になる。
7. 片瀬小学校は青色・余裕あり・顔マーク1体になる。
8. その他の施設は黄色・普通・顔マーク2体になる。
9. JSON、データベース、テンプレートなど対象システムのデータ形式が壊れていない。
10. 登録画面から、任意の空き状況と複数設備を指定して登録できる。
11. 登録した避難所を検索すると、登録した空き状況と設備タグが表示される。
12. 不正な設備キーを送信しても保存されない。
13. 実装後に、対象画面のルートまたは画面テストを実行して動作確認する。

最後に、変更したファイル、実装した内容、実行した検証結果だけを簡潔に報告してください。
